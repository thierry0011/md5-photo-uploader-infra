template-file-path: templates/05-ecr.yaml
parameters:
  ProjectName: photo-gallery
  Environment: dev
  # Leave blank on first deploy. After 06-github-oidc.yaml is deployed, copy
  # its GitHubActionsRoleArn output here and let Git sync redeploy this stack
  # so the ECR repository policy grants that role push access.
  GitHubActionsRoleArn: ""
tags:
  Project: photo-gallery
  Environment: dev
  ManagedBy: CloudFormation-GitSync

template-file-path: templates/07-ecs-alb.yaml
parameters:
  ProjectName: photo-gallery
  Environment: dev
  ContainerPort: "8000"
  TestListenerPort: "8080"
  TaskCpu: "512"
  TaskMemory: "1024"
  # Bootstrap placeholder; CodeDeploy replaces this with the real ECR image
  # on the first successful pipeline run.
  ContainerImage: "public.ecr.aws/docker/library/httpd:2.4"
  DjangoAllowedHosts: "*"
tags:
  Project: photo-gallery
  Environment: dev
  ManagedBy: CloudFormation-GitSync
